This directory is not empty!

It was compressed using 'gitar'.
Open a console (terminal) and enter

    $ cd <this directory>
    $ gitar restore

Now you can work with your files.

If you're done with editing your files, enter

    $ gitar update

Your changes will be added to the repository,
the directory will look empty again.